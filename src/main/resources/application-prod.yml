app:
  datasource:
    jdbcUrl: jdbc:postgresql://postgres:5436/abc
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin


spring:
  flyway:
    locations: classpath:/db/migration/postgres
  profiles:
    active: "production"




